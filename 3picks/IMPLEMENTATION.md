# 3PICKS 홈페이지 구현 명세

```text
SKILL: 3picks-design v1.3
MODE: cream (사용자 최종 지시로 white-flatlay에서 전환)
REFERENCES: tokens.css | components.html | objects.html | precision-qa.md | pack-baseline.md
```

## 입력 정본

- 기능: `../굿즈사이트-기능정의-v1.md`
- 제품 요구: `../굿즈사이트-PRD-v1.md`
- 상품: `../굿즈리스트 전달용/[최종본] 상품마스터.xlsx`
- 이미지: `../흰배경상품/`
- 브랜드: `../3PICKS-logo-final.svg`
- 대표 니즈: 제품별 3색 예시, 100개 대표 상품 밖의 품목도 소싱·제작한다는 안내, 추천 후 1:1 카카오톡 상담 전환

## 섹션 매핑표

| # | 섹션 | 정보유형 | 시각문법 | 구현 컴포넌트 |
|---:|---|---|---|---|
| 0 | 제작·상담 안내 | PROSE | 마르퀴 티커 | `tp-ticker` |
| 1 | GNB·카테고리 | HIERARCHY·RELATION | 스크롤스파이·이동형 트랙 + 공통 3CTA; 모바일도 대분류 4개 상시 노출, 메뉴 언어는 한글로 통일 | `site-nav`, `main-links`, `category-strip`, `cta-actions` |
| 2 | 키비주얼 | RELATION·PROSE | 티셔츠 그래피티와 제목을 유지하고 좌측 하단 여백에 문장 길이별 먹색 라벨 4개 배치; 상품 확장 안내는 카탈로그 도입부로 이동; 모바일 이미지 우선 | `hero__backdrop` + `hero__copy` + `hero__copy-stack` |
| 3 | 핵심 서비스 안내 | METRIC·FLOW | 낮은 4칸 픽토그램 신뢰 바 | `proof-strip` |
| 4 | 맞춤 추천 설문 | FLOW | 한 화면 한 질문·스텝 레일 | `survey-shell`, `progress-rail` |
| 5 | 추천 결과 | HIERARCHY·RELATION·COMPARE | 요약과 팀 일러스트 뒤, 구성별로 `설명 | 1st pick | 2nd pick | 3rd pick`을 한 가로 행에 배치 | `result-intro`·`result-group`·4칸 제로갭 비교 행 |
| 6 | 전체 카테고리 | DISTRIBUTION·PROSE | 100개 대표 상품 대제목 아래 상품 확장 안내를 2열 산문 띠로 연결하고, 10개 가로 상품 레일마다 색상 안내를 한 번만 배치 | `catalog-intro` + `catalog-promise` + `tp-grid--floor` + `category-color-note` |
| 7 | 제작 과정·추천 기준 | TIME·TABLE | 단계별 기준을 합친 4열 진행 레일 | `tp-timeline` |
| 8 | 상담 전환 | PROSE·RELATION | 오렌지 팝 서피스 + 공통 3CTA | `tp-pop-surface`, `cta-actions` |
| 9 | FAQ | TABLE·PROSE | 5개 룰 리스트·질문/답변 좌측 정렬 | `tp-faq` |
| 10 | 멤버 소개 | HIERARCHY·PROSE·RELATION | 전폭 2행 대제목 + 좌측 2단 카피 + 우측 2인 팀 포트레이트 + 스토리 끝의 원래 4줄 먹 반전 카피 | `about-layout`, `about__content`, `about__copy`, `about-team`, `member-story__ending` |
| 11 | 푸터 | TABLE·PROSE | 연락처·임시 사업자 정보 행 | `site-footer` |
| 12 | 위시리스트·미니 견적 | RELATION·TABLE·METRIC | 좌하단 트리거에서 가로로 펼쳐지는 고정 견적 레일 | `quote-float`, `quote-bar`, A4 인쇄 문서 |

## 정밀 기준표

| 대상 | 잠금 값 |
|---|---|
| 캔버스 | 전면 크림 `#F5F0E4`; 흰색 배경 면 제거 |
| 공통 레일 | 최대 1200px; 거터 `clamp(16px, 2.4vw, 32px)` |
| 간격 | 4px 체계; 섹션 `clamp(64px, 8vw, 96px)` |
| 구획선 | `#A89F91` 1px; 제로갭 컨테이너 배경 + 1px gap |
| 상품 카드 | 1101px 이상 5열 가시 폭, 901~1100px 4열, 900px 이하 2열, 600px 이하 2.2장(2장 전체+다음 카드 20%) 가시 폭; 데스크톱 카드 약 239px·내부 패딩 16px 유지·상품명 14px·가격 값 17px; 미디어 1:1; 모바일 상품명 13px·가격 16px, 가격 세로 적층·메타 값 열 줄바꿈 허용; 3색 버튼은 카드 하단 기준선에 고정하고 색상 안내 문구는 상품별 반복 없이 각 카테고리 레일 아래 한 번만 표시 |
| 추천 결과 | 데스크톱은 좌측 카피와 조건 변경 버튼·270px 팀 일러스트 뒤, 구성별 한 행을 `minmax(240px,1fr) + 3×1fr`로 잠금; 인트로는 기존 360px 대비 가로·세로 75% 비례 축소한 일러스트로 약 25% 낮추고, 조건 변경 버튼은 주황 요약 셀 좌·하단에서 각각 24px; 첫 칸은 구성가·잔액/부족액·이유, 나머지는 `1st·2nd·3rd pick`; 900px 이하는 요약 카피 바로 아래 버튼→일러스트 뒤 각 구성을 2×2(`설명·1st / 2nd·3rd`) 테이블로 재배치; 가로 스크롤 없음 |
| 설문 | 데스크톱 2열 선택지, 모바일 1열; 컨트롤 48px·터치 44px 이상 |
| FAQ | 질문 5개; 질문/답변 좌측 정렬; 데스크톱 질문 22px·답변 16px, 600px 이하 질문 18px; 1px 행 구분선 유지 |
| 타이포 | 히어로 `clamp(56px, 6.2vw, 88px)`; 히어로 하단 라벨 `clamp(24px, 2.4vw, 32px)`; 본문 16px; 읽는 메타 14px; 라벨 13px |
| 색 | 유채색은 오렌지 `#FC6B38`, 옐로 `#FFFD78`만 사용; 오렌지 면 내부 요소는 검정·흰색만 사용 |
| 반응형 | 900px 이하 GNB 대분류 4개를 테이블 선·셀 배경 없이 44px 이상 텍스트 행으로 재배치·히어로 이미지→카피·설문 1열·추천 요약→일러스트→구성·상품 2열 가시; 멤버 소개는 제목→카피→2인 포트레이트 순서; 600px 이하 상품 레일은 2.2장 노출·고정 모바일 CTA |
| 이미지 | 상품은 560×560 WebP·`loading=lazy`; 히어로 실파일은 1671×941 알파 WebP·`fetchpriority=high`; 멤버 실루엣은 1122×1402 알파 PNG |
| 히어로 배치 | 데스크톱은 전체 히어로에 `contain center`·90% 스케일, 카피는 왼쪽 44% 오버레이; 900px 이하는 이미지 영역을 문서 흐름의 첫 번째에 둠; 600px 이하는 원본 알파 가시 경계 `983×911`(x 657~1639, y 12~922)를 기준으로 그림 폭을 `100vw`에 맞추고 좌우 거터 없이 풀블리드 |
| 히어로 보조 카피 | 부모 면 없이 `찾고,`·`비교하고,`·`보고하는 귀찮은 일.`·`이제 3PICKS에 시키세요.` 네 문장만 각각 `max-content` 먹색 사각 라벨로 구성; 데스크톱 좌측 하단, 모바일 본문 다음; 같은 문구는 스토리 끝의 원래 검정 상자에도 유지 |
| 카탈로그 확장 안내 | 대제목 아래 상·하단 1px 선 사이에 원문 두 문단을 보존; 1101px 이상은 큰 약속 14 : 체크 안내 11의 2열과 현재 2줄·1줄 고정, 1100px 이하는 1열; 왼쪽 22~26px·오른쪽 15~17px, 옐로는 32px 체크 신호 한 곳에만 사용 |
| 신뢰 바 | 히어로 직후 1200px 레일의 4열·최소 88px; 세로선 없이 24~64px 열 여백; 900px 이하 2×2; 아이콘 28px·스트로크 1.2px·본문 13px 이상 |
| 상·하단 CTA | `맞춤추천 → 카카오톡 상담하기 → 이메일문의` 순서와 색 역할 고정; 상단은 헤더 위계를 위해 13px·40px 높이·6px 간격으로 축소, 하단은 48px 유지; 600px 이하 상단 CTA를 로고 아래 전용 행으로 재배치하고 44px 터치 목표 유지 |
| 카테고리 메뉴 | 데스크톱은 별도 `width`를 두지 않고 `.tp-wrap`의 1200px 공통 레일을 그대로 상속해 검은 설문 패널 시작선과 상단 `이메일문의` 끝선에 양쪽 정렬; 10개 칩만 내부에서 가변 분배하며 900px 이하에서는 내용폭·가로 스크롤로 전환 |
| 모션 | 240ms 호버, 320ms 카테고리 트랙 이동; reduced-motion에서 티커·전환 정지 |
| 미니 견적 | 좌우 24px·하단 24px 고정, 72px 사각 트리거, 데스크톱 높이 208px, 상품 영역만 가로 스크롤, 260ms 무바운스 가로 전개, 외곽선 4면 연속; 열린 상태에서 견적 영역 밖 `pointerdown` 시 접힘, 견적 내부 조작과 상품 하트의 열기 클릭은 유지 |
| 모바일 브랜드 스토리 | 사용자 모바일 캡처 기준; 900px 이하 6컷 모두 공통 레일의 중앙·100% 폭, 원본과 같은 4:3 미디어, 컷 사이 32px; 좌우 혼합 오프셋과 3:4 세로 미디어 박스는 사용하지 않음 |
| 멤버 소개 | 전폭 대제목 56~68px 뒤 콘텐츠 행을 좌측 카피 5 : 우측 인물 7로 배치; 핵심 카피 30~34px 2행·실행 설명 22~26px 4행; 두 인물은 우측 정렬 2열, 상반신 아래 1px 선만 유지; 외곽·중앙·정보 행 선과 경력 목록은 제거; 900px 이하는 제목→카피→인물 1열 |

## 상품 데이터 계약

- 상품마스터와 `assets/products-data.js`의 원본 입력은 110개이며 표준 상품 이미지는 제품당 3장씩 330장이다. 원본 데이터와 이미지는 삭제하지 않는다.
- 사이트 운영 집합은 10개 카테고리별 `rank 1~10`, 총 100개다. 카탈로그는 이 100개를 `visibility`와 무관하게 카테고리마다 10개씩 모두 표시한다.
- 맞춤 추천은 같은 100개 안에서 정확 조건을 먼저 적용한다. 결과가 3안 미만이면 제작 가능·가격·공직자 1인 5만 원 조건은 유지하고, MOQ·납기·인쇄·제외 품목을 상담 확인 후보로 단계적으로 넓혀 빈 결과를 만들지 않는다.
- 공통 카테고리 순서는 `텀블러 → 에코백 → 볼펜 → 우산 → 티셔츠·단체복 → 수건·타올 → 머그컵 → 보조배터리 → 노트·다이어리 → 보온보냉·런치백`이며 수건·타올은 06번이다.

## 구현 판단

- 히어로는 기존 보조배터리를 티셔츠로 교체한 가로형 그래피티 일러스트를 사용한다. 데스크톱에서는 카피를 왼쪽 44%에 오버레이하고, 900px 이하에서는 키비주얼을 먼저 보여준 뒤 카피를 잇는다.
- 600px 이하의 키비주얼은 공통 레일 거터를 벗어나 `100vw`를 채운다. 원본 캔버스의 투명한 왼쪽 영역이 아니라 실제 오브젝트 경계 983px을 화면 폭에 맞추고, 가시 영역 중심점 x=1148.5px을 화면 중심에 고정한다.
- 히어로 종이 바탕은 CSS 혼합으로 보정하지 않고 알파 WebP에서 제거한다. 페이지의 실제 `#F5F0E4`가 그림 사이로 직접 보이므로 이미지 사각 경계가 생기지 않는다.
- 히어로 직후의 대형 숫자 카드 스트립은 삭제 상태를 유지한다. 대신 `100개 대표 상품·더 넓은 상품 소싱·맞춤 제작·견적과 납기 안내`를 낮은 4칸 신뢰 바로 배치한다.
- 추천 설문의 섹션 헤드는 결과 약속(`우리에게 딱 맞는 굿즈는?`), 검은 패널은 조작 안내(`맞춤 추천 입력`), 질문 영역은 실제 질문으로 역할을 분리한다. 첫 화면에서 의문사 `어떤`은 `어떤 행사를 준비하고 계신가요?` 한 곳에만 둔다.
- 총예산 입력은 `원` 대신 `만원` 단위를 사용한다. 사용자가 `100`을 입력하면 상태에는 `1,000,000원`으로 저장해 기존 추천 필터·1인당 예산·결과 문구 계산을 그대로 유지한다.
- 추천 카테고리는 조건을 통과한 대표 상품이 1개 이상이면 결과에 포함한다. 비교 상품 2개가 모두 있어야만 카테고리를 통과시키던 기존 조건은 저예산에서 전체 결과를 숨기므로 제거하고, 부족한 비교 상품은 사실대로 덜 보여준다.
- 이전 세션에 `100`처럼 만원 표시값이 저장된 경우에는 복원·추천 직전에 `1,000,000원`으로 보정한다. 10,000 이상의 기존 원 단위 저장값은 그대로 유지한다.
- 검은 패널 제목은 행사별 조건부 질문으로 7~8단계가 달라지는 점을 반영해 고정 숫자 대신 `맞춤 추천 입력`으로 표시한다.
- 마스터의 `전체마스터`는 헤더 포함 111행이며 실제 상품은 110개다. 이 원본과 표준 이미지 330장은 보존하고 사이트만 rank 기준 100개로 제한한다.
- 상품 카드의 3색은 색상을 고르는 데 도움을 주는 이미지 예시다. 모든 카드에 `실제 제작 가능 색상은 상담 후 확정됩니다`를 고정 표기한다.
- 카카오톡 상담 CTA는 사용자가 제공한 `https://open.kakao.com/me/3PICKS`을 `config.js` 한 곳에서 관리하고 새 창으로 연다. 주소가 비어 있을 때만 기존 담당자 연락처 안내 대화상자로 대체한다.
- 헤더와 하단 상담 전환은 서로 다른 2버튼 조합을 폐기하고 `맞춤추천·카카오톡 상담하기·이메일문의`의 동일한 3버튼 조합으로 통일한다. 추천은 `#recommend`, 카카오는 기존 `data-consult`, 이메일은 `config.js`의 담당자 이메일로 연결한다.
- 정확 조건 뒤 3안이 남지 않으면 상담 조정 가능한 후보를 보충하고, 완화된 MOQ·납기·인쇄·제외 품목을 각 구성에 명시한다. 버튼 선택만으로 끝나는 모든 경로는 반드시 3안을 반환한다.
- 마스터에 트렌드 태그 전용 열이 없어 `상품성격·특징/비고·카테고리` 텍스트에서 12개 태그를 보수적으로 파생한다. 추천 이유에는 이 파생 사실을 노출하지 않고 행사·예산·납기·로고 조건만 설명한다.
- 가격·MOQ·납기를 숫자로 해석할 수 없는 상품은 전체 카탈로그에는 표시하되 자동 추천에서는 제외한다. 하드 필터를 통과했다고 과장하지 않기 위한 보수적 처리다.
- 새로고침 시 앵커 해시보다 저장된 세로 위치를 우선하고, 복원 직전에 URL 해시를 제거해 설문 위치로 다시 끌려가지 않게 한다.
- 추천이 끝나면 설문 답변과 3개 추천 그룹의 상품 ID를 탭 세션에 저장하고, 새로고침 탐색에서 같은 순서로 복원한다. `조건 다시 입력하기`를 누르면 저장 결과도 함께 지운다.
- 모든 상품 카드의 우상단 하트는 동일 상품 ID로 동기화하고 최대 12개를 로컬 저장한다. 담을 때 합계 포함이 기본이며 체크 해제는 보관만 유지하고 계산·PDF·상담 요약에서 제외한다.
- 미니 견적은 브라우저 하단에 길고 낮은 가로 프레임으로 고정해 본문을 계속 볼 수 있게 한다. 공통 수량은 100개 기본·20개 단위이며 상품별 MOQ보다 작으면 적용 수량만 MOQ로 올린다.
- 상품 금액은 현재 기준 단가×적용 수량의 참고 합계다. 인쇄·배송·부가세는 구조화된 값이 아니므로 숫자 합계에 넣지 않고 상담 확인 문구로 남긴다.
- `PDF 미니 견적서`는 A4 인쇄 전용 새 창을 만들고 브라우저의 인쇄·PDF 저장 기능을 호출한다. 견적 바 자체는 팝업으로 전환하지 않는다.
- GNB 가장 오른쪽의 `멤버 소개`는 FAQ 다음의 `#about`으로 이동한다. 백선미 이사의 실제 경력과 소개를 텍스트로 제공하고, 두 번째 멤버는 가짜 이력 없이 준비 중 더미로 둔다.
- 푸터에는 교체용 임시값 `사업자등록번호 000-00-00000`, `통신판매업 신고번호 제2026-서울강남-0000호`를 두며 실제 번호를 받으면 두 줄만 바꾼다.

## 2026-08-10 추천 로직 교정

- 예산 상한 단품 추천을 예산 차이 최소화 1~3종 구성 추천으로 교체했다.
- 결과에 구성가와 예산 잔액·부족액을 표시하고 `예산 아직 모름` 경로를 추가했다.
- 상품마스터 전수 대조와 96개 추천 시나리오 회귀 검사를 자동화했다.

## 2026-08-11 전 경우의 수 보강

- 입력 인원과 상품별 MOQ를 반영한 총 가예산으로 조합을 정렬한다.
- 행사·조건부 질문·로고·태그·제외 품목·공직자와 수치 경계 5,786개를 검사한다.
- 결과 마지막에 재고·인쇄·포장·배송에 따른 금액 변동 안내를 친절한 문장으로 표시한다.

## 2026-08-11 운영 집합·콘텐츠 개편

- 사이트 운영 상품을 카테고리별 rank 1~10, 총 100개로 제한하고 카탈로그와 추천의 노출 조건을 분리했다.
- 수건·타올을 06번으로 이동하고 티셔츠 히어로, 모바일 이미지 우선, 멤버 소개, 임시 사업자 정보를 연결했다.

## 2026-08-12 추천 구성 행 구조 개편

- 세로로 긴 구성 3열은 어느 상품이 어느 구성에 속하는지 즉시 읽기 어렵다는 사용자 캡처 피드백을 반영한다.
- 정보 계층을 `구성 행 → 설명 셀 → 1st·2nd·3rd pick`으로 고정하고, 제품이 3개 미만인 구성은 없는 제품을 지어내지 않고 오른쪽 빈 셀로 남긴다.
- 설명 셀은 큰 번호 `01·02·03` 아래 기계적인 `구성 01·02·03`을 반복하지 않고 `첫번째 구성.·두번째 구성.·세번째 구성.`으로 읽히게 한다.

## 2026-08-12 상·하단 4줄 카피 동시 유지

- 히어로 좌측 하단의 독립 검정 라벨 4개를 유지한다.
- 스토리 끝 검정 상자에도 원래 `찾고, / 비교하고, / 보고하는 귀찮은 일. / 이제 3PICKS에 시키세요.` 제목을 복원한다.
- 데스크톱은 카피와 상담 버튼의 2열, 600px 이하는 카피 다음 전폭 버튼의 1열 구조를 쓴다.

## 2026-08-12 모바일 브랜드 스토리 정렬 교정

- 사용자 실기기 캡처에서 4:3 원본을 3:4 박스에 `contain`으로 표시해 이미지 아래가 빈 공간처럼 늘어난 문제를 확인했다.
- 900px 이하에서는 6컷 모두 좌우 오프셋 없이 공통 레일을 채우고 중앙 정렬하며, 미디어 비율을 4:3으로 통일한다.
- 모바일 컷 사이는 32px로 줄이되 이미지와 캡션은 한 패널의 인플로우 구조를 유지한다.

## 2026-08-12 카탈로그 확장 안내 재배치

```text
SKILL: 3picks-design v1.3
MODE: cream
REFERENCES: components | precision-qa
```

- 인벤토리: 히어로의 상품 확장 안내 두 문단, 카탈로그의 100개 대표 상품 대제목, 상품 레일 10개를 다룬다.
- 논리·문법: 두 문단은 `PROSE`이므로 문장을 고치지 않고 보더 프레임 산문 띠로 옮긴다. 카탈로그 레일은 기존 `DISTRIBUTION` 문법을 유지한다.
- 리듬: 대형 제목 다음에 낮은 2열 산문 띠를 두고, 그 아래 상품 카드가 시작되게 해 타이포→산문→상품의 높낮이를 만든다.
- 정밀 기준: 공통 1200px 레일, 4px 간격 체계, 그리지 1px 선, 옐로 체크 신호 한 곳, 900px 이하 1열 전환, 본문 14px 이상을 잠근다.

## 2026-08-12 키비주얼 좌측 두 덩어리 확대

- 좌상단 제목은 최대 76px에서 88px로, 좌하단 검정 문구는 최대 24px에서 32px로 키워 중앙의 과도한 빈 면을 줄인다.
- 두 덩어리의 상·하단 고정 위치는 보존하고, 하단 문구의 행 간격과 안쪽 여백을 함께 키워 네 줄이 한 덩어리로 읽히게 한다.
- 900px 이하에서는 하단 24px, 600px 이하에서는 제목 최대 58px·하단 20px로 별도 축소해 가로 넘침을 막는다.

## 2026-08-12 최소 주문 수량 한글 표기

- 고객 화면의 `MOQ`를 모두 `최소 주문 수량`으로 바꾼다.
- 상품 카드뿐 아니라 추천 결과·미니 견적함·PDF 견적서·FAQ에도 같은 한글 용어를 쓴다.

## 2026-08-12 어드민 상품 등록·수정·숨김·보관 (site-overrides v2)

- 병합 규칙을 `recommendation-core.js`로 승격해 `OVERRIDABLE_PRODUCT_FIELDS`(15필드)·`PRODUCT_STATUSES`·`mergeSiteProducts`·`filterActiveProducts`·`activeSiteProducts`를 홈페이지와 어드민이 공유한다. app.js의 병합 사본은 제거했다.
- `site-overrides.js`를 v2로 올려 `productAdditions`(신규 상품 전체 레코드)를 추가했다. 원본과 id가 겹치는 등록 레코드는 병합에서 무시해 원본을 보호한다.
- 숨김·보관은 상태 필터 후 카테고리 순위를 1부터 재정렬해, 빠진 자리를 아래 순위가 자동 승격으로 채운다. 삭제 버튼은 정책상 만들지 않았다(회귀 테스트가 감시).
- 어드민 반영은 사진(장당 1커밋, base64 PUT)을 먼저 올리고 설정을 마지막에 커밋한다. 중간 실패 시 설정은 그대로라 재시도로 이어진다. 교체 사진은 `-vN` 파일명으로 캐시를 회피한다.
- 사진 변환은 브라우저 canvas로 560×560 흰 바탕 WebP q82(미지원 브라우저는 PNG 폴백)를 만들고, 반영 전 초안은 dataURL로 localStorage에 저장하며 용량 초과 시 배지로 경고한다.
- 홈페이지 쪽이 콘솔 밖(LLM 등)에서 바뀌면 상품·기본값·설정의 지문 대조와 원격 설정 비교로 감지해, 헤더의 「LLM으로 홈페이지 수정한 내용 어드민에 적용하기」 버튼을 빨간 깜빡임으로 알리고 수동 클릭 시에만 적용한다. 연결 확인의 원격 설정 자동 적용은 이 감지·수동 방식으로 대체했다.
- 회귀: `test_admin_pairing.js`에 core 정본 참조·상태 enum 검사, `test_site_overrides.js`에 v2 스키마·확장 필드, 신규 `test_product_registration.js`에 등록 순위 삽입·숨김 승격·보관 제외·관리 화면 계약을 추가했다.

## 2026-08-12 상품 카드 고객용 문구 정리

```text
SKILL: 3picks-design v1.4
MODE: homepage
REFERENCES: current homepage contract | precision QA
```

- 인쇄 방식은 표시 단계에서 자르지 않고, 운영 문구 자체를 최대 30자 안에서 완결된 표현으로 편집한다.
- `recommendation-core.js`의 `PRODUCT_FIELD_LIMITS.printMethod = 30`을 홈페이지·어드민의 공통 정본으로 삼는다. 어드민 수정·신규 등록 입력창은 `maxlength=30`을 적용하고 저장 검사도 같은 값을 쓴다.
- 최소 주문 수량과 인쇄 문구의 `**`·`★`·`[번호]` 같은 편집 기호를 운영 오버라이드에서 제거하고, 어드민 저장 단계에서도 다시 유입되지 않게 막는다.
- 공급처는 내부 리서치·운영 데이터로만 유지한다. 홈페이지 상품 카드와 고객용 견적 출력물에서는 공급처를 표시하지 않는다.
- 하트 버튼의 기본 사각 테두리는 가격 아래 구분선과 같은 `rgba(0,0,0,.12)`로 낮추고, 활성 상태의 옐로 채움은 유지한다.

## 2026-08-12 색상 옵션·어드민 최대 5장 확장

- 상품의 사진·색상 라벨 배열은 같은 순서를 공유하며 어드민에서 최대 5개까지 등록·교체·수정한다. 기존 1~3개 상품도 그대로 저장할 수 있다.
- 색상명이 상품명에 포함되는 상품은 고정 상품명 대신 `기본 상품명 · 선택 색상`으로 표시하고, 어드민의 `선택한 색상명을 상품 제목에 표시` 값으로 같은 동작을 관리한다.
- 고객 화면의 상품명에는 공급처용 품번·시리얼 코드를 노출하지 않으며, 어드민의 수정·신규 등록 저장 검사에서도 재유입을 막는다.
- 샤인투톤 레이저 머그는 공식 옵션에 맞춰 옐로우·레드·그린·브라운·블랙 5개 사진과 라벨을 연결한다.
